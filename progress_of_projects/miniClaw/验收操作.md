## 一、限制工作区域的方法

1.将`"C:\Users\25283\.nanobot\config.json"`的**restrictToWorkspace**改为true

2.并将`"C:\Users\25283\.nanobot\config.json"`的**agents.defaults.workspace**更改成相应的文件夹

## 二、对工具调用过程中的异常进行捕获和友好提示

见代码：
E:\githubrepository\bot\nanobot\nanobot\agent\tools\filesystem.py中的execute函数总共四个，对于异常进行了捕获和提示

## 三、拓展建议

- [x] 对话历史持久化：将对话记录保存到本地 JSON 文件，下次启动时可加载历史

  C:\Users\25283\.nanobot\workspace\sessions

- [x] 工具插件化：支持通过配置文件或装饰器动态注册新工具，无需修改核心代码

  使用说明:`E:\githubrepository\bot\nanobot\nanobot\agent\tools\plugin_loader.py `

  添加工具插件的方法：

  工具设置格式参照：`(E:\githubrepository\bot\nanobot\plugin_tools_test\myTools.py)`

  config.json配置：在toolPlugins中添加对应的插件工具；

  测试：

  1.手动创建了新工具hello_tool`(E:\githubrepository\bot\nanobot\plugin_tools_test\myTools.py)`

  2.在对话框中说：请用 hello_plugin 工具，参数 msg 为 manual-test, 告诉我调用工具后完整的返回信息；

  3.结果检查:若返回[hello_plugin] manual-test，则插入成功

- [x] 流式输出：支持 LLM 的流式响应，逐字输出（而不是一次性返回）

- [x] 多轮规划展示：在控制台清晰展示每轮的“思考→行动→观察”过程

- [x] 安全的 shell 执行沙箱：对 run_shell_command 实现更严格的命令白名单和参数校验

  配置：`C:\Users\25283\.nanobot\config.json`中exec.allowedCommands添加相应的白名单和参数校验

  命令校验流程：先匹配 `denyPatterns`（内置危险模式 + 你自定义的）→ 再走 `allowedCommands` → 再 `allowPatterns`（ `.*`等价于不挡）→ 内网 URL → `restrictToWorkspace` 路径

  denypatterns的校验函数在`E:\githubrepository\bot\nanobot\nanobot\agent\tools\shell.py`

  演示：【丢弃】在`C:\Users\25283\.nanobot\config.json`中的exec.allowedCommands的`del` / `rm` / `rmdir` / `erase`删掉，然后再命令行中输入“删除‘测试.txt’”，会反馈”删除操作不在allowedCommand中“；重新加回删除指令则可以正常删除

- [x] 支持多 Agent 协作雏形：实现一个简单的“主 Agent + 子 Agent”模式（如主 Agent 可将文件分析任务委托给专门的分析子 Agent）

直接在命令行输入

```txt
用 spawn 工具把[task]交给子 Agent，label填[name]

eg: 用 spawn 工具把查询今日北京天气的任务交给子 Agent，label填weather
```