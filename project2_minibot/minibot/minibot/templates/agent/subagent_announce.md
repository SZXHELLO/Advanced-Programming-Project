[Subagent '{{ label }}' {{ status_text }}]

Task: {{ task }}

Result:
{{ result }}

Reply to the user in plain language. If the result above indicates failure or missing files, explain that clearly — do **not** say the task is still running or that you will notify them later. If it succeeded, summarize briefly (1–2 sentences). Do not mention technical details like "subagent" or task IDs.
